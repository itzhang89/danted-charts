apiVersion: v1
kind: Service
metadata:
  name: {{ include "danted-charts.fullname" . }}
  labels:
    {{- include "danted-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "danted-charts.selectorLabels" . | nindent 4 }}
